# 📊 SQL Logic Building: 180+ Queries Mastery

This repository is a dedicated log of 180+ SQL queries solved to sharpen my analytical thinking and database logic. It demonstrates my ability to handle various data scenarios using structured query language.

## 📌 Project Objective
The core focus of this practice was to move beyond basic syntax and master the logic required for complex data extraction and business reporting.

## 🛠️ Technical Logic & Concepts Practiced

### 1. Data Analysis & Filtering
- Implementing precise filtering using `WHERE`, `BETWEEN`, `IN`, and `LIKE` patterns.
- Managing data sorting and ranking using `ORDER BY` and `LIMIT`.

### 2. Aggregation & Business Metrics
- Calculating key metrics (SUM, AVG, MIN, MAX) to derive insights.
- Categorizing data using `GROUP BY` and filtering aggregated results with `HAVING`.

### 3. Relational Data Logic
- Mastering table relationships using `INNER JOIN`, `LEFT JOIN`, and `RIGHT JOIN`.
- Handling multi-table architecture to fetch consolidated information.

### 4. Advanced Analytical Queries
- Writing **Subqueries** for nested logic.
- Using **Common Table Expressions (CTEs)** for cleaner and more readable complex queries.
- Implementing conditional logic using **CASE Statements**.

### 5. Database Management (DDL/DML)
- Creating and altering table structures.
- Efficiently managing records using `INSERT`, `UPDATE`, `DELETE`, and `TRUNCATE`.

## 📂 Repository Structure
- `SQL 150+ marathon.sql`: A comprehensive script containing all practice questions and solutions, organized by logical progression.

## 💡 Key Takeaways
- Developed a "Data-First" mindset for solving problems.
- Achieved accuracy in writing complex join and aggregation logic.
- Focused on writing clean, readable, and optimized SQL code.

---
**Focus:** SQL Logical Development & Data Analytics Foundations
